import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class Master {
  // store data
  // create api call function 70%
  // create subject or behaviotrSubject
  // utility function or helper function

  addTowNum(num1: number, num2: number) {
    return num1 + num2;
  }
}
